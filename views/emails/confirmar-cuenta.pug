doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body 
        h2 Confirma tu Cuenta UpTask 
        p 
            | Hola, ya estás casi inscrito, solo presiona
            | el siguiente enlace para confirmar
            a(href=`${confirmarUrl}`) Confirmar Cuenta
        p Si no puedes acceder a ese enlace, visita #{confirmarUrl}